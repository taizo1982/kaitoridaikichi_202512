{
  "name": "lp-renewal-magi",
  "private": true,
  "type": "module",
  "version": "0.0.0",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "magi:init": "node scripts/magi-init.mjs",
    "magi:sync": "node scripts/magi-sync.mjs"
  },
  "dependencies": {
    "@aivec/magi-lp-template": "*"
  },
  "devDependencies": {
    "vite": "^6.0.0"
  }
}
